@using TravelProject1.Models.Classlar
@model List<Blog>


<div class="padding1">
    <h4>Ən Sevdiyim Yerlər</h4>
    <div class="box-left">
        @foreach (var x in Model)
        {
            <div class="bot-img">
                <img src="@x.BlogImg" style="width:200px;height:75px;" alt="" />
            </div>
            <div class="bot-desc">
                <p>@x.Aciqlama.Substring(0,75)...</p>
            </div>
            <div class="clear"></div>
        }
    </div>
    @Html.Action("Partial5","Home")
</div>
